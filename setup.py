from setuptools import setup

setup(
   name='ngrokbot',
   version='0.1.0',
   description='Ngrok bot manager',
   author='Eduardo Filho',
   author_email='efdsfilho@gmail.com',
   packages=['ngrokbot'],
#   install_requires=['python-telegram-bot'],
)
